% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-grade-target.R
\name{plan_grade_target}
\alias{plan_grade_target}
\title{Plan a grade target under a quality-band payoff}
\usage{
plan_grade_target(
  quality_draws,
  actions,
  value,
  quality_shift = NULL,
  cost = 0,
  crop = "wheat",
  grounding = decideR::grounding_unverified(),
  safe_action = NULL,
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{quality_draws}{A numeric vector of posterior draws of the baseline
quality outcome under the status-quo action.}

\item{actions}{A numeric vector of candidate input levels (the grid the
decision chooses over).}

\item{value}{A \code{decideR::grade_band_value} schedule pricing the quality
bands -- typically \code{\link[=wheat_protein_bands]{wheat_protein_bands()}} or \code{\link[=barley_malting_premium]{barley_malting_premium()}}.}

\item{quality_shift}{A function \verb{function(action, quality)} returning the
realised quality under the action, or \code{NULL} for the additive default
\code{quality + action}.}

\item{cost}{A per-unit input cost (a single number applied as
\code{cost * action}), a function \verb{function(action)}, or \code{0} for no cost.}

\item{crop}{The crop the decision concerns (default \code{"wheat"}).}

\item{grounding}{One or more grounding tokens for the quality evidence;
combined worst-case.}

\item{safe_action}{The action returned on abstention (default the first
element of \code{actions}, the status quo).}

\item{decisive_prob}{Minimum posterior probability that the leading action
beats the status quo (passed to \code{decideR::decide()}).}

\item{...}{Further arguments passed to the decision core (e.g. \code{min_ess}).}
}
\value{
A \link{grain_decision} of kind \code{"grade_target"} whose \code{action} is the
recommended input level.
}
\description{
Choose how hard to push a quality-driving input when the grain payoff steps
at a delivery-standard threshold (a wheat protein band, a barley malting
window) rather than rising smoothly. It is a grain-semantics wrapper over
\code{decideR::decide()} driven by a grade-band loss: the value schedule prices
each quality band, the utility is the band payoff of the shifted quality
outcome less the input cost, and the expected utility decideR averages over
the draws is governed by each band's posterior probability -- exactly the
threshold economics a quality-graded decision faces. It abstains to the
status-quo action when the quality evidence is unverified or no action is
clearly better.
}
\details{
\code{quality_draws} is a numeric vector of posterior draws of the baseline
quality outcome (grain protein, say) under no extra input. \code{actions} is the
candidate input grid (a nitrogen-rate grid, say). \code{quality_shift(action, quality)} returns the realised quality under an action -- the additive
default \code{quality + action} suits a unit that lifts quality one-for-one, and a
supplied function suits a saturating or partial response. \code{value} is a
grade-band schedule from grainPlan's value library (or any
\code{decideR::grade_band_value()}), and \code{cost} prices the input.
}
\examples{
set.seed(1L)
# Baseline protein draws; a nitrogen top-up lifts protein additively.
protein <- rnorm(2000L, mean = 11.0, sd = 0.6)
v <- wheat_protein_bands()
plan_grade_target(protein, actions = seq(0, 60, by = 10), value = v,
                  quality_shift = function(rate, p) p + 0.02 * rate,
                  cost = 1.3, crop = "wheat",
                  grounding = decideR::grounding_grounded())
}
\seealso{
\code{\link[=wheat_protein_bands]{wheat_protein_bands()}} and \code{\link[=barley_malting_premium]{barley_malting_premium()}} for the value
schedules; \code{\link[decideR:grade_band_utility]{decideR::grade_band_utility()}} for the loss; \code{\link[decideR:decide]{decideR::decide()}}
for the engine.

Other grain-decisions: 
\code{\link{plan_nitrogen_rate}()},
\code{\link{plan_nitrogen_rate_from_manifest}()},
\code{\link{plan_variety}()},
\code{\link{plan_variety_from_manifest}()}
}
\concept{grain-decisions}

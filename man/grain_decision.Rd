% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grain-classes.R
\name{grain_decision}
\alias{grain_decision}
\title{A grain decision -- a recommended grain action with context and grounding}
\usage{
grain_decision(
  decision = NULL,
  crop = NA_character_,
  kind = NA_character_,
  action = NULL,
  unit = NA_character_,
  abstained = FALSE,
  grounding = "[unverified]",
  rationale = NA_character_,
  context = list()
)
}
\arguments{
\item{decision}{The wrapped \code{decideR::decision} object.}

\item{crop}{The crop the decision concerns (e.g. \code{"wheat"}, \code{"barley"},
\code{"corn"}).}

\item{kind}{The decision kind, one of \code{"nitrogen_rate"}, \code{"variety"}, or
\code{"grade_target"}.}

\item{action}{The recommended action, lifted from the wrapped decision (the
\code{safe_action} when the decision abstained).}

\item{unit}{A short string naming the units of \code{action} (e.g. \code{"kg N/ha"},
\code{"variety"}, \code{"protein \%"}).}

\item{abstained}{\code{TRUE} when the wrapped decision declined the
utility-maximal action.}

\item{grounding}{A canonical grounding token, copied from the wrapped
decision.}

\item{rationale}{A short human-readable sentence explaining the
recommendation, suitable for a grower-facing report.}

\item{context}{A named list of free-form grain context (prices, the
candidate set, the dataset attribution).}
}
\value{
A \code{grain_decision} S7 object.
}
\description{
The unit grainPlan recommends: exactly one \code{decideR::decision} wrapped with
the grain context that gives it agronomic meaning. Where a bare \code{decision}
records "action 90, expected utility 1840, grounded", a \code{grain_decision}
records "apply 90 kg/ha of nitrogen to this wheat crop, grounded, profit
$1840/ha" -- the same decision, read in grain terms.
}
\details{
The object carries the wrapped \code{decision} whole (in \code{decision}) so nothing
decideR computed is lost, and lifts the fields a practitioner reads first to
the top: the \code{crop}, the decision \code{kind}, the recommended \code{action} and its
\code{unit}, whether the decision \code{abstained}, and the \code{grounding} token. The
grounding is copied from the wrapped decision, never recomputed, so a
\code{grain_decision} can never claim a stronger provenance than the decision it
wraps.
}
\examples{
set.seed(1L)
rates <- seq(0, 150, by = 25)
ymax  <- rnorm(800L, 5.5, 0.3)
yld   <- vapply(rates, function(r) 3 + (ymax - 3) * (1 - exp(-r / 70)),
                numeric(800L))
gd <- plan_nitrogen_rate(yld, rates, price_grain = 350, price_n = 1.3,
                         crop = "wheat",
                         grounding = decideR::grounding_grounded())
gd@action
}
\seealso{
\link{grain_plan} for a season of decisions; \code{\link[=plan_nitrogen_rate]{plan_nitrogen_rate()}},
\code{\link[=plan_variety]{plan_variety()}}, and \code{\link[=plan_grade_target]{plan_grade_target()}} for the verbs that build it.

Other grain-objects: 
\code{\link{grain_is_grounded}()},
\code{\link{grain_plan}()},
\code{\link{grain_plan_table}()},
\code{\link{plan_season}()}
}
\concept{grain-objects}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-nitrogen-rate.R
\name{plan_nitrogen_rate_from_manifest}
\alias{plan_nitrogen_rate_from_manifest}
\title{Plan a nitrogen rate from an upstream orchestra manifest}
\usage{
plan_nitrogen_rate_from_manifest(
  manifest,
  rates,
  price_grain,
  price_n,
  crop = "wheat",
  safe_rate = 0,
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{manifest}{An \code{orchestra_manifest} carrying a yield-per-rate draws
matrix (in \code{outputs} or \code{metadata$yield_draws}), one column per rate.}

\item{rates}{A numeric vector of candidate nitrogen rates, one per column of
the manifest's draws matrix.}

\item{price_grain}{The grain price per unit yield.}

\item{price_n}{The nitrogen price per unit rate.}

\item{crop}{The crop the decision concerns (default \code{"wheat"}).}

\item{safe_rate}{The fallback rate on abstention (default \code{0}).}

\item{decisive_prob}{Minimum posterior probability that the leading rate
beats the status quo.}

\item{...}{Further arguments passed to the decision core (e.g. \code{min_ess},
\code{draws_key}).}
}
\value{
A \link{grain_decision} of kind \code{"nitrogen_rate"} whose \code{action} is the
recommended rate.
}
\description{
The manifest entry point to the nitrogen-rate decision: source the yield
draws and the producer's grounding token straight from an
\code{orchestra_manifest} emitted upstream (a PESTO inversion, a TACI
yield-response posterior), price them, and return a \link{grain_decision}. It is
the formalised crop-pipeline tail at the grain layer -- a yield posterior in,
a profit-priced, grounding-aware nitrogen-rate plan out -- built directly on
\code{decideR::decide_rate_from_manifest()}, which reads the manifest by its
public contract (the draws matrix and the grounding token) without grainPlan
depending on the contract package.
}
\details{
The grounding rides from the producer through the decision: an unverified
yield manifest forces the status-quo rate. grainPlan takes no dependency on
the manifest constructor; the manifest is read by decideR's duck-typed tail.
}
\examples{
\dontrun{
# `manifest` is an orchestra_manifest S7 object emitted upstream (a PESTO
# inversion or a TACI yield-response posterior), carrying a yield-per-rate
# draws matrix in its `outputs` slot (or `metadata$yield_draws`) and a
# grounding token in `metadata$grounding`. decideR's tail reads it by its
# public S7 contract, so grainPlan needs no dependency on the contract package.
rates <- seq(0, 200, by = 25)
plan_nitrogen_rate_from_manifest(manifest, rates,
                                 price_grain = 350, price_n = 1.3)
}
}
\seealso{
\code{\link[=plan_nitrogen_rate]{plan_nitrogen_rate()}} for the caller-assembled-matrix form;
\code{\link[decideR:decide_rate_from_manifest]{decideR::decide_rate_from_manifest()}} for the underlying tail.

Other grain-decisions: 
\code{\link{plan_grade_target}()},
\code{\link{plan_nitrogen_rate}()},
\code{\link{plan_variety}()},
\code{\link{plan_variety_from_manifest}()}
}
\concept{grain-decisions}

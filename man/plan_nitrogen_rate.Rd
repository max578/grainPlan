% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-nitrogen-rate.R
\name{plan_nitrogen_rate}
\alias{plan_nitrogen_rate}
\title{Plan a nitrogen rate for a grain crop}
\usage{
plan_nitrogen_rate(
  yield_draws,
  rates,
  price_grain,
  price_n,
  crop = "wheat",
  grounding = decideR::grounding_unverified(),
  safe_rate = 0,
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{yield_draws}{A numeric matrix of posterior yield draws, one column per
candidate rate (rows are draws).}

\item{rates}{A numeric vector of candidate nitrogen rates, one per column of
\code{yield_draws}.}

\item{price_grain}{The grain price per unit yield (passed to
\code{\link[=n_rate_economics]{n_rate_economics()}}).}

\item{price_n}{The nitrogen price per unit rate (passed to
\code{\link[=n_rate_economics]{n_rate_economics()}}).}

\item{crop}{The crop the decision concerns (default \code{"wheat"}).}

\item{grounding}{One or more grounding tokens for the yield evidence;
combined worst-case and passed to the decision engine.}

\item{safe_rate}{The fallback rate on abstention (default \code{0}, the no-input
status quo).}

\item{decisive_prob}{Minimum posterior probability that the leading rate
beats the status quo (passed to \code{decideR::decide()}).}

\item{...}{Further arguments passed to the decision core (e.g. \code{min_ess}).
\code{constraint} is rejected with an error: \code{decideR::decide_input_rate()}
does not honour a rate constraint and would otherwise drop it silently
(use \code{\link[=plan_nitrogen_rate_from_manifest]{plan_nitrogen_rate_from_manifest()}}, which does honour one, or cap
\code{rates} before calling).}
}
\value{
A \link{grain_decision} of kind \code{"nitrogen_rate"} whose \code{action} is the
recommended rate.
}
\description{
The canonical grain decision: choose how much nitrogen to apply to a crop by
expected profit, abstaining to the status-quo rate when the yield evidence is
unverified or no rate is clearly better. It is a grain-semantics wrapper over
\code{decideR::decide_input_rate()} -- the profit maximisation, the grounding
firewall, and the abstention logic are decideR's; grainPlan supplies the
grain economics (\code{\link[=n_rate_economics]{n_rate_economics()}}), the crop context, a grower-facing
rationale, and the \link{grain_decision} result.
}
\details{
Pass either a yield-draws matrix (\code{yield_draws}, one column per candidate
rate, rows are posterior draws -- the object a PESTO inversion or a TACI
yield-response posterior produces) together with \code{rates}, or an upstream
\code{orchestra_manifest} to \code{\link[=plan_nitrogen_rate_from_manifest]{plan_nitrogen_rate_from_manifest()}}, which sources
the draws and the grounding token straight from the manifest. Either way the
grounding rides from the evidence through the decision: an unverified yield
posterior forces the status-quo rate, never a confident wrong recommendation.
}
\examples{
set.seed(1L)
rates <- seq(0, 200, by = 25)
# diminishing-returns yield response with posterior noise
mu <- 3 + 2.5 * (1 - exp(-rates / 80))
yield_draws <- vapply(mu, function(m) rnorm(1500L, m, 0.3), numeric(1500L))
plan_nitrogen_rate(yield_draws, rates, price_grain = 350, price_n = 1.3,
                   crop = "wheat",
                   grounding = decideR::grounding_grounded())
}
\seealso{
\code{\link[=plan_nitrogen_rate_from_manifest]{plan_nitrogen_rate_from_manifest()}} for the manifest entry point;
\code{\link[=n_rate_economics]{n_rate_economics()}} for the economics; \code{\link[decideR:decide_input_rate]{decideR::decide_input_rate()}} for
the underlying engine.

Other grain-decisions: 
\code{\link{plan_grade_target}()},
\code{\link{plan_nitrogen_rate_from_manifest}()},
\code{\link{plan_variety}()},
\code{\link{plan_variety_from_manifest}()}
}
\concept{grain-decisions}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grain-value-library.R
\name{barley_malting_premium}
\alias{barley_malting_premium}
\title{Barley malting-premium value schedule}
\usage{
barley_malting_premium(
  window = c(9.5, 12.5),
  feed_price = 280,
  malting_premium = 60
)
}
\arguments{
\item{window}{A length-two numeric vector \code{c(low, high)} giving the malting
protein window in percent (default \code{c(9.5, 12.5)}).}

\item{feed_price}{The feed price in dollars per tonne paid outside the
window (default an illustrative \code{280}).}

\item{malting_premium}{The premium in dollars per tonne added to the feed
price inside the window (default an illustrative \code{60}).}
}
\value{
A \code{decideR::grade_band_value} schedule: a vectorised function from a
protein outcome to its dollar-per-tonne payoff, paying
\code{feed_price + malting_premium} inside the window and \code{feed_price} outside.
}
\description{
The barley counterpart to \code{\link[=wheat_protein_bands]{wheat_protein_bands()}}: a price per tonne that
carries a malting premium when grain protein falls inside the malting window
and drops to the feed price outside it. Malting barley is the textbook
two-sided quality payoff -- protein that is too low or too high both miss the
malting grade -- which a single rising step schedule cannot express, so this
schedule pays the premium only across the central band.
}
\details{
The default \code{window} is the broad malting protein window in percent and the
default prices are an illustrative feed price and malting premium in dollars
per tonne. The window shape (a premium inside an acceptance band, feed
outside) follows the published Barley Australia / Grain Trade Australia
malting receival conventions; the dollar values are illustrative and not
receival prices. Supply current prices for a real decision.
}
\examples{
v <- barley_malting_premium()
v(c(8.0, 10.5, 13.0))
v
}
\references{
Barley Australia and Grain Trade Australia malting barley
receival standards (the malting protein window). Dollar values are
illustrative.
}
\seealso{
\code{\link[=wheat_protein_bands]{wheat_protein_bands()}} for the wheat counterpart;
\code{\link[=plan_grade_target]{plan_grade_target()}} for the decision that consumes it.

Other grain-value:
\code{\link[=n_rate_economics]{n_rate_economics()}},
\code{\link[=wheat_protein_bands]{wheat_protein_bands()}}
}
\concept{grain-value}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grain-value-library.R
\name{wheat_protein_bands}
\alias{wheat_protein_bands}
\title{Wheat protein-band value schedule}
\usage{
wheat_protein_bands(breaks = c(10.5, 11.5, 13), values = c(300, 330, 360, 400))
}
\arguments{
\item{breaks}{A strictly increasing numeric vector of protein thresholds in
percent (default \code{c(10.5, 11.5, 13.0)}, the milling / hard / prime-hard
edges).}

\item{values}{A numeric vector of dollar-per-tonne band payoffs, of length
\code{length(breaks) + 1}, ordered from the lowest (feed) band up (default an
illustrative \code{c(300, 330, 360, 400)}).}
}
\value{
A \code{decideR::grade_band_value} schedule: a vectorised function from a
protein outcome to its band payoff in dollars per tonne, carrying the
schedule for inspection.
}
\description{
The canonical Australian milling-wheat payoff: a price per tonne that steps
up as grain protein clears each receival band. This is the schedule the
orchestra runs kept hand-building when they priced a quality-graded wheat
decision; \code{wheat_protein_bands()} consolidates it into a first-class,
documented \code{decideR::grade_band_value()} schedule so a decision can price
protein without re-deriving the band table.
}
\details{
The default \code{breaks} are the protein thresholds that separate the broad
Australian wheat grades by protein -- feed below the milling floor, then the
standard milling grades (Australian Standard White, Australian Premium White)
and the high-protein hard grades (Australian Hard, Australian Prime Hard) --
and the default \code{values} are an illustrative step-up of dollars per tonne
across them. The band shape (a step at each protein threshold) follows the
published Grain Trade Australia wheat standards; the dollar values are
illustrative and not receival prices. Supply current prices for a real
decision.
}
\examples{
v <- wheat_protein_bands()
v(c(9.5, 11.0, 11.5, 13.4))
v
}
\references{
Grain Trade Australia, \emph{Wheat Standards} (publicly published
receival standards; band thresholds), and the broad Australian wheat grade
classes (AGW Feed, ASW, APW, AH, APH). Dollar values are illustrative.
}
\seealso{
\code{\link[=barley_malting_premium]{barley_malting_premium()}} for the barley counterpart;
\code{\link[=plan_grade_target]{plan_grade_target()}} for the decision that consumes it.

Other grain-value: 
\code{\link{barley_malting_premium}()},
\code{\link{n_rate_economics}()}
}
\concept{grain-value}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-variety.R
\name{plan_variety}
\alias{plan_variety}
\title{Plan a variety choice from a multi-environment trial or genomic posterior}
\usage{
plan_variety(
  merit_draws,
  varieties = NULL,
  risk_aversion = 0.5,
  incumbent = NULL,
  crop = "wheat",
  grounding = decideR::grounding_unverified(),
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{merit_draws}{A numeric matrix of posterior merit draws, one column per
variety (rows are draws).}

\item{varieties}{A character vector of variety names, one per column of
\code{merit_draws}; defaults to the column names of \code{merit_draws}, then to
\code{"v1"}, \code{"v2"}, ....}

\item{risk_aversion}{A non-negative number weighting the one-sided downside
penalty (default \code{0.5}); \code{0} is plain expected merit.}

\item{incumbent}{The variety recommended on abstention -- the status-quo line
a grower keeps when the evidence does not justify a switch; a variety name
in \code{varieties}, or \code{NULL} to default to the first variety.}

\item{crop}{The crop the decision concerns (default \code{"wheat"}).}

\item{grounding}{One or more grounding tokens for the merit evidence;
combined worst-case.}

\item{decisive_prob}{Minimum posterior probability that the leading variety
beats the incumbent (passed to \code{decideR::decide()}).}

\item{...}{Further arguments passed to the decision core (e.g. \code{min_ess}).}
}
\value{
A \link{grain_decision} of kind \code{"variety"} whose \code{action} is the
recommended variety name; the ranked short-list is in
\code{decision@candidates} of the wrapped decision.
}
\description{
Rank candidate grain varieties under a downside-aware merit loss and
recommend the leading one, abstaining when the merit evidence is unverified
or no variety is clearly better than the incumbent. It is a grain-semantics
wrapper over \code{decideR::decide()}: the variety indices are the candidate
actions, the utility is each variety's posterior merit less a one-sided
downside penalty (\code{risk_aversion} times the shortfall below the variety's own
mean, so a high-yielding but volatile line is discounted against a steady
one), and the grounding firewall plus abstention are decideR's.
}
\details{
\code{merit_draws} is a numeric matrix of posterior merit draws with one column
per variety (rows are draws) -- the object a factor-analytic MET model emits
(a variety's mean across environments) or a genomic-prediction model emits
(its genomic estimated breeding value posterior). The recommendation is the
single leading variety; the full ranked ledger of every variety's expected
merit is carried in the wrapped decision's \code{candidates} for a short-list.

The grounding rides from the evidence: an unverified merit posterior forces
the recommendation to the \code{incumbent} variety (the safe action), so a variety
is never switched on evidence the producer did not ground.
}
\examples{
set.seed(1L)
varieties <- c("Scepter", "Vixen", "Calibre", "Denison")
means <- c(5.2, 5.4, 4.9, 5.3)
merit_draws <- vapply(means, function(m) rnorm(1000L, m, 0.25),
                      numeric(1000L))
plan_variety(merit_draws, varieties, crop = "wheat",
             grounding = decideR::grounding_grounded())
}
\seealso{
\code{\link[=plan_variety_from_manifest]{plan_variety_from_manifest()}} for a \code{breeding_values} manifest;
\code{\link[decideR:decide]{decideR::decide()}} for the underlying engine.

Other grain-decisions:
\code{\link[=plan_grade_target]{plan_grade_target()}},
\code{\link[=plan_nitrogen_rate]{plan_nitrogen_rate()}},
\code{\link[=plan_nitrogen_rate_from_manifest]{plan_nitrogen_rate_from_manifest()}},
\code{\link[=plan_variety_from_manifest]{plan_variety_from_manifest()}}
}
\concept{grain-decisions}

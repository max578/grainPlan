% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan-variety.R
\name{plan_variety_from_manifest}
\alias{plan_variety_from_manifest}
\title{Plan a variety choice from a breeding-values manifest}
\usage{
plan_variety_from_manifest(
  manifest,
  genotypes = NULL,
  risk_aversion = 0.5,
  incumbent = NULL,
  gebv_sd = NULL,
  n_draws = 1000L,
  crop = "wheat",
  grounding = NULL,
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{manifest}{A \code{breeding_values} \code{orchestra_manifest} (or any S7 object
exposing \code{outputs} and \code{metadata} with a \code{gebv} payload).}

\item{genotypes}{A character vector of genotype names; defaults to the names
on the GEBV payload, then to \code{"g1"}, \code{"g2"}, ....}

\item{risk_aversion}{A non-negative downside-penalty weight (default \code{0.5}).}

\item{incumbent}{The genotype recommended on abstention, or \code{NULL} for the
first.}

\item{gebv_sd}{Optional per-genotype GEBV standard deviation used to
reconstruct a posterior from point GEBVs (a single number or one per
genotype). When \code{NULL} (default) the uncertainty is read from the manifest
(PEV, standard error, or reliability plus genetic variance); when the
manifest carries none, supply this rather than let the function guess.
Ignored when the payload is a draws matrix.}

\item{n_draws}{The number of draws to reconstruct per genotype when the
payload is point GEBVs (default \code{1000L}).}

\item{crop}{The crop the decision concerns (default \code{"wheat"}).}

\item{grounding}{Optional override for the manifest's own grounding token;
defaults to the token the manifest carries.}

\item{decisive_prob}{Minimum posterior probability that the leading genotype
beats the incumbent.}

\item{...}{Further arguments passed to \code{\link[=plan_variety]{plan_variety()}}.}
}
\value{
A \link{grain_decision} of kind \code{"variety"} whose \code{action} is the
recommended genotype name.
}
\description{
The genomic entry point to the variety decision: read genomic estimated
breeding values (GEBVs) straight from a \code{breeding_values}
\code{orchestra_manifest} -- the manifest flexyBayes's genomics targets emit
(\code{genomic_summary()} lifted through the composition layer) -- and rank the
genotypes under the same downside-aware merit loss \code{\link[=plan_variety]{plan_variety()}} uses.
This closes the genomics-to-decision loop: a GBLUP posterior in, a
grounding-aware genotype recommendation out.
}
\details{
The manifest is read by its public contract. GEBVs are sought in
\code{outputs$gebv} (the \code{breeding_values} payload shape) as either a numeric
vector of point GEBVs (one per genotype) or a matrix of GEBV draws (genotypes
in columns). The grounding token rides from the manifest exactly as the
nitrogen tail does -- an unverified breeding-values manifest forces the
incumbent. grainPlan takes no dependency on the manifest constructor; the
manifest is read by duck-typed S7 property access, the composition-layer
convention.

When the manifest carries point GEBVs rather than a draws matrix, the
downside-aware loss still needs a distribution to weigh, so a per-genotype
normal posterior is reconstructed with the GEBV as its mean. The standard
deviation is the genotype's own genomic uncertainty, resolved in this order
and never fabricated silently:
\enumerate{
\item an explicit caller \code{gebv_sd} (a single value or one per genotype) if
supplied, which always wins;
\item a per-genotype prediction-error variance on the manifest
(\code{outputs$pev} or \code{metadata$pev}), used as \eqn{sd_j = \sqrt{PEV_j}};
\item a per-genotype standard error on the manifest (\code{outputs$gebv_se},
\code{outputs$se}, or \code{metadata$gebv_sd}), used directly;
\item a per-genotype reliability (\code{outputs$reliability} or
\code{metadata$reliability}) together with an additive-genetic variance
(\code{metadata$genetic_var}), used as \eqn{sd_j = \sqrt{\sigma^2_a\,(1-r^2_j)}}
from the standard reliability-PEV relation (Mrode, 2014).
}
When none of these is available the function stops rather than invent an
uncertainty scale a point GEBV cannot carry: a genotype switch must not turn
on a magic constant. The resolved source is recorded in the decision's
\code{context} as \code{gebv_sd_source}, and the reconstruction never strengthens the
grounding.
}
\examples{
\dontrun{
# `manifest` is a breeding_values orchestra_manifest S7 object -- the manifest
# flexyBayes's genomics targets emit (genomic_summary lifted through the
# composition layer), carrying GEBVs in `outputs$gebv` and a grounding token
# in `metadata$grounding`. The manifest is read by duck-typed S7 property
# access, so grainPlan needs no dependency on the contract package.
plan_variety_from_manifest(manifest, risk_aversion = 0.5)
}
}
\references{
Mrode, R. A. (2014). \emph{Linear Models for the Prediction of Animal
Breeding Values} (3rd ed.). CABI. The reliability-PEV relation
\eqn{r^2 = 1 - PEV/\sigma^2_a}.
}
\seealso{
\code{\link[=plan_variety]{plan_variety()}} for the caller-assembled-matrix form.

Other grain-decisions: 
\code{\link{plan_grade_target}()},
\code{\link{plan_nitrogen_rate}()},
\code{\link{plan_nitrogen_rate_from_manifest}()},
\code{\link{plan_variety}()}
}
\concept{grain-decisions}
